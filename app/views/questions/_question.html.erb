<li class="list-group-item" id="question_<%= question.id %>">
  <span class="badge"><%= question.responses.count %></span>
  <%= link_to question.title, question_path(question) %><%= link_to '<i class="glyphicon glyphicon-trash pull-right"></i>'.html_safe, question_path(question), method: :delete, id: "delete_question_#{question.id}", remote: true %>
</li>
